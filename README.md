# Go Auth

This is a quick and dirty example of using GoLang to signup and authenticate a user.<br/>
The app uses local MySql as a database.<br/>
The script to create the sample table is included on this project -> db/users.sql<br/>
Inside contains a signup and a login page.

##Requirements
- Crypto bcrypt
- Go mysql driver
